interface:
  display_name: "PocketBase CLI"
  short_description: "Remote PocketBase admin and data tasks via the custom CLI"
  default_prompt: "Use $pocketbase-cli for deployed PocketBase tasks through the custom CLI, including login, CMS or config inspection, collections, records, files, logs, backups, install or repair, then inspect schema, run preflight, and execute safely."

policy:
  allow_implicit_invocation: true
